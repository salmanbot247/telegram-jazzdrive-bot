[phases.setup]
nixPkgs = ["nodejs_20", "python311", "ffmpeg"]

[phases.install]
cmds = [
  "ln -sf $(which python3) /usr/local/bin/python",
  "npm i",
  "pip3 install yt-dlp"
]

[start]
cmd = "node index.js"
