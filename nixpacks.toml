[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x", "python311", "ffmpeg"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m pip install yt-dlp",
  "npx playwright install chromium",
  "npx playwright install-deps chromium"
]

[start]
cmd = "node index.js"

